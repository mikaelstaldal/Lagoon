#!/bin/sh

# UNIX shell script for running Lagoon GUI

# export LAGOON_HOME=/usr/local/lagoon

java -classpath ${CLASSPATH};${LAGOON_HOME}/lib/lagoon.jar;${LAGOON_HOME}/lib/xmlutil.jar nu.staldal.lagoon.LagoonGUI %1 %2 %3 %4 %5 %6 %7 %8 %9
