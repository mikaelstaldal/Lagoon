#!/bin/sh

# UNIX shell script for running Lagoon GUI

# export LAGOON_HOME=/usr/local/lagoon

java -classpath ${CLASSPATH}:${LAGOON_HOME}/lagoon.jar:${LAGOON_HOME}/xmlutil.jar:${LAGOON_HOME}/lsp.jar nu.staldal.lagoon.LagoonGUI $*
